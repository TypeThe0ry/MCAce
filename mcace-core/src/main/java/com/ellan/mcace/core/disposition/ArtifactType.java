package com.ellan.mcace.core.disposition;

/** A client or server artifact to which an operational policy can apply. */
public enum ArtifactType {
    MOD, RESOURCE_PACK, SHADER_PACK, CONFIG, CLIENT, PROTOCOL, SESSION, BEHAVIOR
}
