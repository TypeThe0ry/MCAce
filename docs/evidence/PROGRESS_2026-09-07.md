# MCAce v0.0.1 progress ledger / 进度台账 — 2026-09-07

This is a source-bound follow-up to `PROGRESS_2026-09-06.md`. It records only
observations made from the authoritative `D:\Projects\MCAce` checkout and the
fresh artifact-commit Matrix run. A chat statement is not treated as a GUI
attestation, client upload, supervisor receipt, or production-authority event.

## Repository identity

- Authoritative checkout: `D:\Projects\MCAce`.
- Branch: `feature/active-pack-integrity`.
- Latest audited documentation descendant before this entry: `474d54fe36a739a7cc0fba3749f2ac593d6454f6`.
- Immutable product/artifact source: `2a6274a9200f2aa195e1238eaddf43650f549a0a`.
- GitHub PR: [#17](https://github.com/TypeThe0ry/MCAce/pull/17), still open Draft;
  the last observed `build` and `windows-contracts` checks were green.
- No `v0.0.1` tag or GitHub Release exists.

## Fresh Matrix V4 evidence

The earlier failed publication attempt was rejected because the Matrix report
was produced from the documentation descendant instead of artifact commit A.
That was corrected without editing evidence fields: an independent worktree at
commit A rebuilt the formal `0.0.1` A bundle, verified that all six JARs were
byte-identical to the current R/A bundle, executed all twelve process cases, and
received a fresh detached receipt from the external Matrix signer.

- A-worktree: `D:\MCAce-matrix-2a6274a`.
- A release bundle manifest SHA-256:
  `642977a69647391483336a80e320ffd6e6846df09de7b3d00e82444a00ce6dc2`.
- Cases: `12/12` passed (`10` stable, `2` beta); all process cleanup counts were
  zero; `release_eligible=true`.
- Trust-root SHA-256:
  `05be3d1ce14b03ab66db85045377f52afa91f03d662a4d70c26468a93bcde223`.
- Supervisor signer key: `mcace-matrix-v4-20260826-9f1c36f44eab`.
- Supervisor receipt SHA-256:
  `878ee99995f46fc63d02ea568dee1f16e11c504546f1bad49732fb912c8e53ad`.
- Matrix report SHA-256:
  `24b4760f5ad89ce755878b076229f47ac28f594dd7a34034987b561c31a0ed1c`.
- Published native index: [`server-version-process-matrix-20260907-2a6274a.json`](server-version-process-matrix-20260907-2a6274a.json).
- Published package contains the index plus six canonical documents and twelve
  raw case reports under
  `server-version-process-matrix/server-version-process-matrix-20260907-2a6274a/`.

The Matrix package is now the strongest source-bound external gate. It does not
close GUI consent, Federation V5, Vulcan V3, Production Authority V4, or
protected main/tag CI.

## Enable MCAce association check

The runtime probe subagent was notified that the operator had enabled MCAce. The
probe left the user's client PID `44064` untouched and performed a read-only
association check. The current client log had no matching MCAce consent,
`Loaded ModList`, resource-pack, `SERVER_CONFIRMED`, cheat/Xray, or upload
markers. Computer Use exposed only browser tabs (`apps=[]`), not the native
Minecraft window, so no screenshot or human GUI attestation was captured.

Therefore the operator's Enable statement remains an unbound pending runtime
observation. It is not promoted to release evidence, and no fixture was
generated to fill the gap.

A follow-up read-only monitor ran against the same client session without
stopping, attaching to, injecting into, or modifying PID `44064`:

- observation window: `2026-09-07T02:53:44.0023808+08:00` to
  `2026-09-07T02:54:32.1517116+08:00` (`48.1` seconds);
- the log grew from `197513` to `198754` bytes, but the appended content still
  contained no MCAce consent, `Loaded ModList`, resource-pack,
  `SERVER_CONFIRMED`, telemetry, report, or upload marker;
- result: `NO_MATCHING_NEW_MCACE_MARKERS`.

This follow-up does not change the gate status: the Enable statement remains
unbound and cannot be promoted to GUI or client-runtime release evidence.

## Current documentation-descendant bundle audit

After commit `2896b35`, the current documentation descendant was rebuilt with
the immutable artifact source `2a6274a9200f2aa195e1238eaddf43650f549a0a`.
The six deployable JARs remained byte-identical to the A bundle; only the
source-bound control manifest changed as expected:

- current release manifest SHA-256:
  `bd2c1c970f86f40f921830438e57a922aac5131ecf5322991c36a4b91b55270e`;
- current `SHA256SUMS` SHA-256:
  `ede01f0068eff5d494f6ed2ff88adc68da8fcbf92c4e3d5a4a21e432998fee45`;
- current compatibility report passed with source `2896b35ef7d9dcb1d9b45a42459ecd5f2bdd7a15`
  and artifact source `2a6274a9200f2aa195e1238eaddf43650f549a0a`;
- the local readiness recheck accepted the Matrix V4 package against this
  descendant and left only GUI/Federation, Vulcan, Production Authority, and
  protected main/tag CI gates pending.

The external Authority signer self-tests passed for both PS7 engines, but the
separate ACL audit currently fails closed on inherited ACLs under
`D:\MCAceReleaseAuthority\archive`; no permission mutation was made during
this audit. Existing external Federation GUI attempts were also not reused:
their signed source/artifact commits and JAR hashes are older than the
current immutable artifact source, so they cannot bind to this release.

## b846ff0 follow-up verification

After commit `b846ff0`, the exact documentation descendant was rebuilt with
the immutable artifact source `2a6274a9200f2aa195e1238eaddf43650f549a0a`.
The Gradle `releaseBundle` completed successfully (`37` tasks), and the
dynamic version contract passed. The resulting control-file identities are:

- current release manifest SHA-256:
  `a7db1a7acdf04c3ed75623da579a61bba833fc6b8bc9963b3012c37d2d66d468`;
- current `SHA256SUMS` SHA-256:
  `ede01f0068eff5d494f6ed2ff88adc68da8fcbf92c4e3d5a4a21e432998fee45`;
- source commit: `b846ff0bcdf430479d9e888a6e946677dbc1de38`;
- artifact source commit: `2a6274a9200f2aa195e1238eaddf43650f549a0a`.

The six deployable JAR hashes remain unchanged from the artifact-source A
bundle. Local release-readiness accepted the fresh Matrix V4 package and
reported the remaining blockers exactly as:

`fabric_gui_single_enablement_confirmation`,
`fabric_federation_real_handoff`, `vulcan_genuine_event`,
`production_server_confirmed_authority`, and `protected_exact_release_bundle`.

The protected exact-bundle gate is intentionally distinct from a local build:
it requires the protected `main`/tag CI context with the exact source-bound
bundle, compatibility report, and artifact-source marker. GitHub Actions runs
for `b846ff0` were observed in progress; no green protected-main/tag result was
promoted before completion.

The latest read-only client association monitor still found no MCAce consent,
`Loaded ModList`, resource-pack, `SERVER_CONFIRMED`, cheat/Xray, telemetry, or
upload marker in the observed client log. The operator's Enable statement is
therefore recorded as an unbound input, not as GUI or runtime release evidence.

The follow-up bundle rebuild for `474d54fe36a739a7cc0fba3749f2ac593d6454f6`
also completed successfully (`37` Gradle tasks). Its source-bound manifest
SHA-256 was `d898241d88ad68f15bdb9ead2ad4d6151ca5bd4d58341fb4b144f36b2c1a8b43`,
while `SHA256SUMS` remained
`ede01f0068eff5d494f6ed2ff88adc68da8fcbf92c4e3d5a4a21e432998fee45`; the
dynamic compatibility contract passed with the same immutable artifact source.

## Fresh controlled anti-cheat correlation rerun

The MCAce-owned executable client/server integration fixture was rerun from
the exact `1ad89b2a7413f55aa677e757ea73c04ecf3dc85d` checkout with Java 21.
It passed for all three target versions (`1.21.11`, `26.1.2`, and `26.2`):
client ModList reporting was observed, an independent server behavior signal
was correlated in the same session, three `SERVER_CONFIRMED` events were
emitted, and the signed lab policy disposition was `QUARANTINE`. The clean
negative boundary remained zero false positives and no clean-client
`SERVER_CONFIRMED` event. The sanitized record is
[`anticheat-live-fixture-20260907-1ad89b2.json`](anticheat-live-fixture-20260907-1ad89b2.json).

This is a real executable loopback integration run, but it is still an
MCAce-owned test JAR rather than third-party cheat code, not a public server
or full Minecraft GUI session, and not evidence of production kick/ban,
Vulcan V3, kernel/DMA, or cross-process memory protection.

## Operator Enable notice and live-profile binding check

The operator subsequently reported that `Enable MCAce` had been completed in
the client. That statement is recorded as an operator input, but it is not
promoted to a GUI attestation until a visible native Minecraft frame and a
source-bound runtime marker are captured.

The read-only profile check identified the following live client state:

- profile: `E:\Ellan艾尔岚-客户端-26.2\.minecraft\versions\EllanTOP-26.2`;
- MCAce JAR present under `mods\mcace-client-fabric-26.2.jar`;
- installed JAR SHA-256:
  `159f4aa8bb2bdc4a223239c9319b86e2ef957030d8c0e9d8c7531a2a925380cc`;
- installed build id: `fabric-26.2-4bb3b57ba3e6c5653c377903b47de7d569afa90f`;
- current release-bundle 26.2 JAR SHA-256:
  `0680a528c38d9f456363488e80ea390615b5ba289714790583a434e2c95b0fe9`.

The installed client artifact is therefore an older source-bound build and
cannot establish evidence for the current c21/A release. Historical compressed
client logs do prove that the older build initialized (`MCACE_FABRIC_ARTIFACT_LOADED`
and `MCAce Fabric client initialized`), but the current `latest.log` has no
post-enable ModList, resource-pack, `SERVER_CONFIRMED`, telemetry, or upload
marker. This is a diagnostic mismatch, not a failed claim about the user's
interaction.

The concurrently running Java server PID `23900` was also bound read-only to a
separate Paper/MinecraftTKV tree (`D:\EllanWorkspace\20260824-ashen-monastery-modeltest`);
its plugin list has no MCAce server JAR. The prepared MCAce GUI server tree
(`D:\MCAce-gui-server-26.2`) is not the active process. No client/server
association is consequently promoted from this observation.

The runtime probe repeated a fresh post-notice incremental observation without
stopping, attaching to, injecting into, or modifying PID `44064`:

- window: `2026-09-07T04:03:38.6509132+08:00` to
  `2026-09-07T04:04:08.8915156+08:00` (`30.2` seconds);
- client log size: `246285` to `246747` bytes;
- the appended bytes contained no enablement acceptance, consent, ModList,
  resource-pack, telemetry, upload, report, or `SERVER_CONFIRMED` marker;
- result: `NO_MATCHING_NEW_MCACE_MARKERS`;
- no `build`-tree `latest.log` was written during the observation window.

This fresh monitor confirms the binding gap after the operator notice; it does
not invalidate the user's interaction, but it cannot close the GUI/runtime
release gate.

## Latest post-notice monitor — 2026-09-07 04:46 +08:00

After the operator again confirmed that `Enable MCAce` had been completed, a
second independent read-only incremental probe watched the same live client
and every `latest.log` under the MCAce federation evidence-run tree. The probe
did not stop, attach to, inject into, or modify PID `44064`.

- window: `2026-09-07T04:46:06.8969238+08:00` to
  `2026-09-07T04:46:37.5008738+08:00` (`30.6` seconds);
- client log: present, ending at `270014` bytes with last write
  `2026-09-07T04:46:05.9669344+08:00`;
- isolated MCAce `latest.log` paths inspected: `38` under
  `D:\Projects\MCAce\build\fabric-federation-gui-handoff\evidence-runs\`;
- appended client bytes contained no `enablement accepted`, `consent accepted`,
  `Loaded ModList`, `active resource pack`, `resource-pack`, telemetry,
  upload, report, or `SERVER_CONFIRMED` marker;
- no isolated server log gained a correlated MCAce event;
- result: `NO_MATCHING_NEW_MCACE_MARKERS`.

This is stronger time-bounded negative evidence after the notice, not a claim
that the human interaction did not happen. The native Computer Use surface
still exposes no Minecraft application (`apps=[]`), and the observed client
profile/artifact remains older than the current source-bound release bundle.
The GUI/runtime evidence gate therefore remains pending.

## 531634a exact v0.0.1 bundle recheck

The latest documentation descendant was rebuilt locally as the formal
`0.0.1` release bundle with root JDK 21, modern JDK 25, source commit
`531634a6bed0f8257be7e1d0d54016be96d6b29d`, and immutable artifact source
`2a6274a9200f2aa195e1238eaddf43650f549a0a`. Gradle `releaseBundle` completed
successfully (`37` actionable tasks); the manifest reports
`product_version=0.0.1`, `release_identity=true`, six deployables, and eight
top-level bundle entries.

The generated six deployables are byte-identified as follows:

```text
mcace-client-fabric-1.21.11.jar  8aef917aed45b03e0f7c2618c9c5b93dfc16f5e048a859183b4680fb29cdc1e2
mcace-client-fabric-26.1.2.jar   410b1ac3bf2081fa2f7abb617d8b101837a7f4141cb11088d4e5606b4e7ac3df
mcace-client-fabric-26.2.jar     0680a528c38d9f456363488e80ea390615b5ba289714790583a434e2c95b0fe9
mcace-server-velocity.jar        a8adc9081b10dc72cc2df7f61f1818ead7bb8f50524a7ad90859587f46e08e16
mcace-server-bungeecord.jar      0bb565bac7d772c95bd8b951720bd7d553df79546f7b45871786329cfdf4b20d
mcace-server-paper.jar            267ab74e7760d344bc154b7ae1051539bb3eeb0c3f17111801aec3142d3156bb
```

The control-file hashes were `DF42279D770A6D8511FC0CFDE6DF976F130973C22971C4E0E120E6DA388940CD`
for `release-manifest.properties` and
`EDE01F0068EFF5D494F6ED2FF88ADC68DA8FCBF92C4E3D5A4A21E432998FEE45` for
`SHA256SUMS`. The version-compatibility contract and the exact-artifact,
anti-cheat fixture, federation-binding, Vulcan-wrapper, and process-matrix
static regressions all passed on this checkout. The local readiness run still
fails closed on the external gates listed below; no tag or release was made.

## Release gate snapshot

Confirmed:

1. exact 1.21.11 / 26.1.2 / 26.2 product bundle and byte hashes;
2. controlled anti-cheat client/server correlation fixture with ModList and
   behavior signal;
3. fresh Matrix V4 A-package, 12/12 process cases, and non-fixture receipt;
4. static compatibility, artifact-policy, anti-cheat, federation-binding,
   Vulcan-wrapper, and process-matrix regression tests.

Still pending and fail-closed:

1. one visible human `Enable MCAce` consent plus signed PNG attestation;
2. real Fabric source-to-target Federation V5 handoff and post-run receipt;
3. licensed Vulcan JAR and genuine external provider event/receipt;
4. Production Authority V4 topology, raw package, and external receipt;
5. protected exact-commit `main` CI, then exact `v0.0.1` tag CI;
6. final README/README_CN evidence pass after the last exact R/A bundle rebuild.

MCAce remains a client telemetry/modlist/resource-pack/behavior correlation
system with server-side disposition. No Tencent ACE-equivalent kernel driver,
DMA detector, cross-process memory scanner, debugger/injection blocker,
public precision/recall, or automatic permanent ban is claimed.

## 6a63733 source-bound release bundle recheck

After the post-enable documentation commits, the exact clean checkout at
`6a6373311d6209b84d7a91f4902a29ca0e441977` was rebuilt with the formal product
version `0.0.1`, root JDK 21, modern JDK 25, and immutable artifact source
`2a6274a9200f2aa195e1238eaddf43650f549a0a`. The Gradle `releaseBundle` task
completed successfully (`37` tasks). The generated bundle passed the V4 shape
and identity checks: six deployables, eight top-level entries, and
`release_identity=true`.

The current control-file identities are:

- release manifest SHA-256:
  `56a77cfc378a33562662a8552f3afca75d2636329ee3d9a8c4f16079cc921e81`;
- `SHA256SUMS` SHA-256:
  `ede01f0068eff5d494f6ed2ff88adc68da8fcbf92c4e3d5a4a21e432998fee45`;
- source commit: `6a6373311d6209b84d7a91f4902a29ca0e441977`;
- artifact source commit: `2a6274a9200f2aa195e1238eaddf43650f549a0a`.

The six deployable hashes are byte-identical to the immutable A bundle:

```text
mcace-client-fabric-1.21.11.jar  8aef917aed45b03e0f7c2618c9c5b93dfc16f5e048a859183b4680fb29cdc1e2
mcace-client-fabric-26.1.2.jar   410b1ac3bf2081fa2f7abb617d8b101837a7f4141cb11088d4e5606b4e7ac3df
mcace-client-fabric-26.2.jar     0680a528c38d9f456363488e80ea390615b5ba289714790583a434e2c95b0fe9
mcace-server-bungeecord.jar      0bb565bac7d772c95bd8b951720bd7d553df79546f7b45871786329cfdf4b20d
mcace-server-paper.jar            267ab74e7760d344bc154b7ae1051539bb3eeb0c3f17111801aec3142d3156bb
mcace-server-velocity.jar        a8adc9081b10dc72cc2df7f61f1818ead7bb8f50524a7ad90859587f46e08e16
```

The dynamic compatibility report was regenerated against the current source
commit and passed. A local release-readiness run then failed closed with the
following remaining gates:

- `MCACE_RELEASE_APPROVED_MATRIX_SUPERVISOR_TRUST_ROOT_SHA256` is not present,
  so the external Matrix V4 supervisor pin cannot be promoted;
- the visible GUI/Federation V5, Vulcan V3, and Production Authority V4
  external evidence packages are still absent or rejected;
- `protected_exact_release_bundle` is intentionally false outside the
  protected GitHub `main`/tag CI context.

At the time of this entry, the newest push and pull-request CI runs for
`6a63733` were still `in_progress`; the older Windows-contract checks were
green. No tag or GitHub Release was created.

## cc37fb1 executable anti-cheat correlation rerun

The current documentation descendant was also exercised through the executable
MCAce-owned client/server loopback fixture. The run completed for all three
supported versions (`1.21.11`, `26.1.2`, and `26.2`) with:

- `client_modlist_reported=true`;
- an independent server behavior signal and same-session correlation;
- `server_confirmed_count=3`;
- signed lab disposition `QUARANTINE`;
- clean-client false-positive count `0` and all three clean negative boundaries
  preserved.

The sanitized record is
[`anticheat-live-fixture-20260907-cc37fb1.json`](anticheat-live-fixture-20260907-cc37fb1.json)
and is bound to source commit
`cc37fb1e5e00f0f99647351cbe5f73e853e3a536`. This remains an executable
loopback integration fixture, not a third-party cheat, full Minecraft GUI,
public server, production kick/ban, Vulcan V3, or kernel/DMA result.

## 71f65d2 local federation proxy matrix — 2026-09-07 05:51 +08:00

The current clean checkout at `71f65d2b23aa2f3ee137fa6531d046ab86768312`
ran the repository's real-process federation matrix with JDK 21:

```text
pwsh -NoProfile -File scripts/federation-proxy-matrix-smoke.ps1 -Pair All
```

The runner exited `0` and produced the raw report at
`build/runtime-federation-matrix/evidence-runs/2026-09-06T21-51-56-3719656Z/report.json`.
Its SHA-256 is
`F0313C11824BB0F3C0ECF6EB81F365B0BF9574134AE3E324FDF6CD52A84F5FEF`.
The normalized, source-bound record is
[`federation-proxy-matrix-20260907-71f65d2.json`](federation-proxy-matrix-20260907-71f65d2.json).

All four real process pairs passed: `Velocity→Velocity`, `Velocity→Bungee`,
`Bungee→Velocity`, and `Bungee→Bungee`. Each case authenticated the source,
stored the grant in process memory, authenticated the target locally, sent a
valid presentation, rejected same-assertion replay, kept source/target audit
healthy, preserved the local trust/risk admission, and observed verified Paper
admission. The logs also recorded signed challenge dispatch, `VERIFIED`
admission, `GRANT_READY`, and login cleanup markers.

This closes the local proxy/backend/process compatibility slice only. The
runner's own raw report leaves `source_commit` and `product_version` empty, so
the repository record binds it explicitly to the clean checkout and the
immutable artifact source instead of treating the runner output as a signed
external receipt. The run remains
`CLIENT_CARRIED_PROCESS_MEMORY_ONLY`, has no source-to-target broker, covers no
Fabric GUI handoff, and does not cover target-restart residuals. The external
Federation V5 source→target GUI handoff, GUI attestation, and independent
post-run receipt remain pending; no release gate was promoted, tag was created,
or GitHub Release published.

## e3ba795 Matrix V4 promotion check — 2026-09-07 06:22 +08:00

The current clean checkout at
`e3ba7951ca5e7449068087d6cc5349674160c784` was rebuilt with the formal
`0.0.1` release bundle task using root JDK 21 and modern JDK 25. The six
deployable JARs remained byte-identical to the externally supervised artifact
source `2a6274a9200f2aa195e1238eaddf43650f549a0a`; only the generated release
manifest source identity advanced to the current documentation descendant.

The bundle controls now report:

```text
release-manifest.properties  sha256=8da7d32de4a4f27ebb1803c55e59acd1790b423739cbc466330365388e0ef8c2  bytes=1969
SHA256SUMS                   sha256=ede01f0068eff5d494f6ed2ff88adc68da8fcf92c4e3d5a4a21e432998fee45  bytes=565
source_commit=e3ba7951ca5e7449068087d6cc5349674160c784
artifact_source_commit=2a6274a9200f2aa195e1238eaddf43650f549a0a
product_version=0.0.1
release_identity=true
bundle_entry_count=8
```

The dynamic compatibility contract was regenerated with the same exact source
and artifact identities and passed. With the out-of-band approved Matrix V4
trust root loaded from
`D:\MCAceReleaseAuthority\public\matrix-v4-trust-root.json` and its pinned
SHA-256, `scripts/release-readiness.ps1` promoted
`server_matrix_exact_source`: all twelve raw/process cases, the current six-
file protected bundle, three server JAR cross-bindings, freshness, replay
rejection, no-follow reads, and the externally signed non-fixture RSA receipt
validated successfully. The readiness report is
`build/release-readiness/report.json` (SHA-256
`8e8c3e84a2dd9140645ded682da1d0feda2e62f0e6aeb900c5101ec55dda41a8`).

This is a real release-gate change, not a claim that all gates are closed. The
same run still fails closed on the visible Fabric GUI/Federation V5 handoff,
licensed Vulcan V3 genuine event, Production Authority V4, and protected
`main`/tag CI context. No tag or GitHub Release was created.

## 2b0c8f2 protected PR CI and Enable notice — 2026-09-07 06:39 +08:00

The current documentation descendant is
`2b0c8f21a9c038941987c163afc2f0ecfa025e70`. GitHub Actions run
`34063871506` for PR #17 completed successfully. Both required PR jobs passed:

```text
windows-contracts  pass  job=101569114910  duration=9m17s
build              pass  job=101570317848  duration=6m43s
```

The build job verified the exact checkout identity, ran the Gradle build/test
suite, built the local verification bundle fully offline, and uploaded that
bundle. The release-candidate, compatibility, and release-readiness steps are
intentionally not treated as protected `main`/tag release CI; the PR run does
not close the final release gates. No tag or GitHub Release was created.

The operator reported that MCAce had been enabled in the visible client. That
notice was forwarded to both runtime-probe Subagents. Their read-only recheck
found no new source-bound consent marker, Loaded ModList, active-resource-pack
observation, telemetry upload, or SERVER_CONFIRMED association, and did not
touch the existing user Minecraft process. Therefore this remains an operator
notice, not GUI release evidence; the visible Fabric consent gate remains
pending until a source-bound record is produced.

## dc5d5b8 executable anti-cheat fixture recheck — 2026-09-07 06:58 +08:00

The current source-bound loopback anti-cheat integration was rerun from
`dc5d5b8e7957a20fcb68fca82be969b010fd2ef7`:

```text
MCACE_ANTICHEAT_LIVE_FIXTURE_EXECUTE_PASS
report_sha256=51fee9971151a29bce79959dfbff666b2b199b6dff9a28dbd611704894169ff2
versions=1.21.11,26.1.2,26.2
client_modlist_reported=true
server_behavior_signal_independent=true
correlated_same_session=true
server_confirmed_count=3
server_confirmed_action=QUARANTINE
clean_false_positive_count=0
```

The static wrapper also passed (`ANTICHEAT_LIVE_FIXTURE_WRAPPER_STATIC_PASS`).
The sanitized record is
[`anticheat-live-fixture-20260907-dc5d5b8.json`](anticheat-live-fixture-20260907-dc5d5b8.json).
This is a controlled executable MCAce client/server loopback fixture: it
proves the modlist signal, independent server signal, same-session correlation,
lab quarantine disposition, and clean-client negative boundaries across all
three supported versions. It is not a third-party cheat/Xray process, not a
full Fabric GUI session, not a public server, and not production kick/ban or
kernel/DMA protection evidence.

## f4df5f5 current bundle/readiness and PR CI — 2026-09-07 07:02 +08:00

The current documentation/evidence descendant is
`f4df5f57293224ecaa25fc54afdc7750fd1c635b`. The local `0.0.1` bundle was
rebuilt with root JDK 21 and modern JDK 25, then checked with the dynamic
compatibility contract. The bundle identity is:

```text
release_identity=true
bundle_entry_count=8
product_version=0.0.1
source_commit=f4df5f57293224ecaa25fc54afdc7750fd1c635b
artifact_source_commit=2a6274a9200f2aa195e1238eaddf43650f549a0a
release-manifest.properties sha256=982b1aaa2c04dcf25193c8b328e9f535c0f5cc7ef7a7936176619a2420f34058 bytes=1969
SHA256SUMS sha256=ede01f0068eff5d494f6ed2ff88adc68da8fcbf92c4e3d5a4a21e432998fee45 bytes=565
compatibility report sha256=587f4130e74f686e9dfb93bcd2e9e638bb99f6d215c126f324ce3a4bf1f50bdb bytes=1486
```

The current readiness report is fail-closed with
`server_matrix_exact_source=true` and `clean_worktree=true`. The remaining
blockers are exactly:

```text
fabric_gui_single_enablement_confirmation
fabric_federation_real_handoff
vulcan_genuine_event
production_server_confirmed_authority
protected_exact_release_bundle
```

For PR #17 at this source, both duplicate GitHub Actions workflows passed:

```text
run=34065567812  windows-contracts=pass  build=pass
run=34065569890  windows-contracts=pass  build=pass
```

These are PR build/contract checks. They do not substitute for the protected
`main`/tag release workflow or for the four missing external authority/GUI
gates. No tag or GitHub Release was created.

## 09c2f0c current PR CI convergence and Enable follow-up — 2026-09-07

The latest documentation descendant is
`09c2f0c31206aa7576a68367de22f21378b0e530`. The operator again reported that
`Enable MCAce` had been completed; that notice was forwarded to both runtime
Subagents. Their read-only follow-up did not find a new source-bound consent
marker, `Loaded ModList`, active-resource-pack observation, MCAce telemetry
upload, or `SERVER_CONFIRMED` association, and the existing user Minecraft
process was left untouched. This remains an operator notice rather than GUI
release evidence.

Both GitHub Actions workflows for PR #17 converged successfully for this
commit:

```text
run=34066423310  windows-contracts=pass  build=pass
run=34066421092  windows-contracts=pass  build=pass
head=09c2f0c31206aa7576a68367de22f21378b0e530
PR=17  state=OPEN  draft=true  mergeStateStatus=CLEAN
```

The second run was a delayed duplicate push-triggered workflow. It remained
stale in `Build the local verification bundle fully offline` for roughly
fifteen minutes; a cancellation request was submitted, and GitHub then
reported its build and contract jobs as passed. The passing PR checks verify
the exact checkout, Windows PowerShell
contract regressions, Gradle build/test suite, offline verification bundle,
and artifact upload. They do not close the protected `main`/tag release gate.

The fail-closed release status is unchanged: Matrix V4 is the only external
release gate currently closed. The visible Fabric consent/Federation V5
handoff, licensed Vulcan genuine event, Production Authority V4, and
protected exact-commit release CI remain pending. No `v0.0.1` tag or GitHub
Release was created.

## d8d2078 current executable anti-cheat fixture — 2026-09-07

The controlled executable client/server loopback fixture was rerun from the
current documentation descendant
`d8d2078e6ecd59d15de1b8e92c60bf4b8f6faa96`:

```text
ANTICHEAT_LIVE_FIXTURE_EXECUTE_PASS
raw_report_sha256=6229758765c691e029e2b6a258335f48c1b0f60c7b7f17c0424eeea774252420
tracked_record_sha256=01adc85a4ef9121fd481e1dc00b9a84666456f5eca02662d19034f69b7a59faf
versions=1.21.11,26.1.2,26.2
client_modlist_reported=true
server_behavior_signal_independent=true
correlated_same_session=true
server_confirmed_count=3
server_confirmed_action=QUARANTINE
clean_false_positive_count=0
```

The static wrapper and the other local release regression wrappers also
passed. The source-bound sanitized record is
[`anticheat-live-fixture-20260907-d8d2078.json`](anticheat-live-fixture-20260907-d8d2078.json).
Its absolute-path report-only verifier passed with the tracked-record hash
above; the raw fixture report and the tracked sanitized record intentionally
have different canonical JSON byte order/formatting.
This strengthens the current-source fixture evidence for the ModList signal,
independent behavior signal, same-session correlation, lab quarantine, and
clean negative boundaries. It remains MCAce-owned executable test code, not a
third-party cheat/Xray process, actual Fabric GUI, public server, production
kick/ban, or kernel/DMA evidence. The GUI/Federation, Vulcan, Production
Authority, and protected release gates remain unchanged.

## 1dd8e13 current evidence and PR CI — 2026-09-07

The current evidence descendant is
`1dd8e1312b0f156b994b4d18a6b727707907a267`. The native Computer Use retry
was attempted for a read-only visible-window inspection, but the local helper
returned `Trusted RPC service is not configured: sky` on both initialization
attempts. No click, process launch, process stop, screenshot capture, or
GUI-state mutation was performed, and the operator's existing Minecraft
process remained untouched. The Enable notice therefore remains oral and is
not a source-bound GUI attestation.

The current-source local regression set passed:

```text
ANTICHEAT_FIXTURE_WRAPPER_STATIC_PASS
ANTICHEAT_LIVE_FIXTURE_WRAPPER_STATIC_PASS
FEDERATION_EVIDENCE_BINDING_TEST_PASS|PowerShell=7.6.5
VULCAN_GENUINE_EVENT_WRAPPER_V3_CONTRACT_PASS|negative_cases=36|fixture_release_eligible=false|v2_diagnostic_only=true
mcace-exact-artifact-policy-tests|status=passed
SERVER_VERSION_PROCESS_MATRIX_STATIC_TEST_PASS
MCACE_VERSION_COMPATIBILITY_STATIC_PASS|dynamic=true|negative_cases=6
RELEASE_READINESS_V5_PASS_WITH_SYMLINK_PERMISSION_GAP|engine=Core-7.6.5
```

For the current commit, the executable anti-cheat fixture raw report hash is
`6229758765c691e029e2b6a258335f48c1b0f60c7b7f17c0424eeea774252420` and the
tracked sanitized record hash is
`01adc85a4ef9121fd481e1dc00b9a84666456f5eca02662d19034f69b7a59faf`; its
absolute-path report-only verification passed.

PR #17 then passed both duplicate workflows for this exact commit:

```text
run=34067676576  event=pull_request  windows-contracts=pass  build=pass
run=34067674921  event=push          windows-contracts=pass  build=pass
head=1dd8e1312b0f156b994b4d18a6b727707907a267
PR=17  state=OPEN  draft=true  mergeStateStatus=CLEAN
```

These are PR/push build and contract checks, not the protected `main`/tag
release workflow. The visible GUI/Federation V5 handoff, licensed Vulcan
genuine event, Production Authority V4, protected exact-commit release CI,
and GitHub Release remain pending. No `v0.0.1` tag or GitHub Release was
created.

## 570ec66 current-head scan and PR validation — 2026-09-07

The current shared D: checkout is clean at
`570ec660fb5e60f8e1f9e4a6e626b8dd1001a58f`. Both read-only subagent rescans
used this exact HEAD and found no new external release material:

```text
NO_NEW_EXTERNAL_MATERIAL
NO_MATCHING_NEW_MCACE_MARKERS
NO_ACTIVE_ISOLATED_CLIENT_PID_49696
NO_PID_44064_LOOPBACK_CORRELATION
NO_SERVER_CORRELATION
```

The operator's existing client process `PID 44064` remained untouched. Its
latest log grew during the observation window, but still contained no
Enable/consent, `Loaded ModList`, active-resource-pack, telemetry, upload,
server-handshake, or `SERVER_CONFIRMED` marker. The installed client JAR
(`159f4aa8bb2bdc4a223239c9319b86e2ef957030d8c0e9d8c7531a2a925380cc`) also
does not match the isolated formal 26.2 artifact
(`0680a528c38d9f456363488e80ea390615b5ba289714790583a434e2c95b0fe9`). The
user's Enable notice is therefore still an operator statement, not a
source-bound GUI attestation or client/server evidence.

The current local bundle is deliberately rejected as a current-HEAD release
bundle because its manifest still says `source_commit=f4df5f5...` while the
checkout is `570ec66...`; its product artifacts remain bound to the frozen
artifact source `2a6274a...`. A current-head readiness run therefore remained
fail-closed on the matrix/current-bundle binding, GUI/Federation, Vulcan,
Production Authority, and protected exact-bundle gates. No old bundle was
rewritten to hide this mismatch.

PR #17 validation for this exact commit completed successfully:

```text
run=34069589278  event=pull_request  windows-contracts=pass  build=pass
head=570ec660fb5e60f8e1f9e4a6e626b8dd1001a58f
PR=17  state=OPEN  draft=true  mergeStateStatus=CLEAN
```

The duplicate push run was cancelled after the PR run was retained for the
merge gate. This is PR validation only, not protected `main`/tag release CI;
no `v0.0.1` tag or GitHub Release was created.

## 848af0f current-source executable fixture rerun — 2026-09-07

The controlled executable client/server loopback fixture was rerun after the
documentation commit so its source binding now points at the current HEAD
`848af0fd0b6aaf0666729a4cd414c303d852a1ab`:

```text
ANTICHEAT_LIVE_FIXTURE_EXECUTE_PASS
raw_report_sha256=717ba7eecc04583dbaf5cfb8eb3855d232c450899fd9d936e4fad38f17653da7
tracked_record_sha256=fab8cf76d6001a531e046e98231b6ae16a1c9de9e46307608c82d0f3ace80984
versions=1.21.11,26.1.2,26.2
client_modlist_reported=true
server_behavior_signal_independent=true
correlated_same_session=true
server_confirmed_count=3
server_confirmed_action=QUARANTINE
clean_false_positive_count=0
```

The report-only verifier passed against the tracked sanitized record
[`anticheat-live-fixture-20260907-848af0f.json`](anticheat-live-fixture-20260907-848af0f.json)
with the tracked hash above. This is current-source MCAce-owned executable
fixture evidence; it is not a third-party cheat/Xray process, actual Fabric
GUI, public server, production kick/ban, or kernel/DMA evidence.

## 28ef1ba final push-check convergence — 2026-09-07

The documentation-only descendant is clean at
`28ef1ba93cc28a63ed8787671b755d50614d97d3`. No product source, release
artifact, or external-evidence claim changed after the `570ec66` scan; this
commit records the final CI state only.

Both the pull-request workflow and the push workflow for this exact commit
completed successfully after the earlier duplicate push attempt was rerun:

```text
run=34070360958  event=pull_request  windows-contracts=pass  build=pass
run=34070357810  event=push          windows-contracts=pass  build=pass
head=28ef1ba93cc28a63ed8787671b755d50614d97d3
PR=17  state=OPEN  draft=true  mergeStateStatus=CLEAN
```

These are feature-branch PR/push checks only. Protected `main` exact-commit
CI, tag CI, the GUI/Federation V5 handoff, licensed Vulcan genuine event,
Production Authority V4, and the current protected release bundle remain
pending. No `v0.0.1` tag or GitHub Release was created.

## 837eb5b final duplicate-workflow convergence — 2026-09-07

The current source/documentation descendant is
`837eb5ba2991842cc4fdf6726c9058bf33fe59e5`. PR #17 is still open and draft,
with `mergeStateStatus=CLEAN`. The pull-request workflow for this exact commit
completed both required jobs successfully:

```text
run=34068394690  event=pull_request  windows-contracts=pass  build=pass
head=837eb5ba2991842cc4fdf6726c9058bf33fe59e5
PR=17  state=OPEN  draft=true  mergeStateStatus=CLEAN
```

The duplicate push run `34068393001` was cancelled to avoid retaining a
redundant concurrent workflow. Its `windows-contracts` and `build` jobs had
already completed successfully before the run-level cancellation was recorded;
the PR check surface remains fully green. This is still PR validation only,
not protected `main`/tag release CI. The GUI/Federation V5 handoff, licensed
Vulcan genuine event, Production Authority V4, protected exact-commit release
CI, and GitHub Release remain pending. No `v0.0.1` tag or GitHub Release was
created.

## 7522b52 current-head V4 bundle rebuild — 2026-09-07

The exact release bundle was rebuilt from the clean documentation-only
descendant `7522b524ab387f47c3ac0b51d70011835baf7921` with the immutable
artifact source pinned to `2a6274a9200f2aa195e1238eaddf43650f549a0a`:

```text
gradlew clean build releaseBundle
--offline --dependency-verification=strict --rerun-tasks
--no-build-cache --no-configuration-cache --no-daemon --no-parallel
--max-workers=1
BUILD SUCCESSFUL in 16m35s
120 actionable tasks: 120 executed
```

The regenerated manifest now binds the requested release descendant while
retaining the artifact-source identity:

```text
source_commit=7522b524ab387f47c3ac0b51d70011835baf7921
artifact_source_commit=2a6274a9200f2aa195e1238eaddf43650f549a0a
product_version=0.0.1
```

All six deployable JARs remained byte-identical to the previously reviewed
artifact set:

```text
mcace-client-fabric-1.21.11.jar  8aef917aed45b03e0f7c2618c9c5b93dfc16f5e048a859183b4680fb29cdc1e2
mcace-client-fabric-26.1.2.jar   410b1ac3bf2081fa2f7abb617d8b101837a7f4141cb11088d4e5606b4e7ac3df
mcace-client-fabric-26.2.jar     0680a528c38d9f456363488e80ea390615b5ba289714790583a434e2c95b0fe9
mcace-server-velocity.jar        a8adc9081b10dc72cc2df7f61f1818ead7bb8f50524a7ad90859587f46e08e16
mcace-server-bungeecord.jar      0bb565bac7d772c95bd8b951720bd7d553df79546f7b45871786329cfdf4b20d
mcace-server-paper.jar            267ab74e7760d344bc154b7ae1051539bb3eeb0c3f17111801aec3142d3156bb
```

The current bundle passed the executable compatibility contract:

```text
MCACE_VERSION_COMPATIBILITY_EXECUTE_PASS
schema=MCACE_VERSION_COMPATIBILITY_CONTRACT_V2
target_count=3
exact_bundle_entry_count=8
unsupported_versions_are_fail_closed=true
passed=true
```

The local readiness report correctly remains fail-closed because the existing
Matrix material is not a valid current protected-bundle publication and the
external GUI/Federation V5, licensed Vulcan V3, Production Authority V4, and
protected `main`/tag CI materials are absent. This rebuild does not claim a
real Fabric GUI consent, third-party cheat/Xray detection, public-server
enforcement, or production authority.
