# MCAce v0.0.1 progress ledger / 进度台账 — 2026-09-07

This is a source-bound follow-up to `PROGRESS_2026-09-06.md`. It records only
observations made from the authoritative `D:\Projects\MCAce` checkout and the
fresh artifact-commit Matrix run. A chat statement is not treated as a GUI
attestation, client upload, supervisor receipt, or production-authority event.

## Repository identity

- Authoritative checkout: `D:\Projects\MCAce`.
- Branch: `feature/active-pack-integrity`.
- Documentation descendant at the time of this update: `0fb15846051c13339aa994f6d3454bb0dedb1958`.
- Immutable product/artifact source: `2a6274a9200f2aa195e1238eaddf43650f549a0a`.
- GitHub PR: [#17](https://github.com/TypeThe0ry/MCAce/pull/17), still open Draft;
  the last observed `build` and `windows-contracts` checks were green.
- No `v0.0.1` tag or GitHub Release exists.

## Fresh Matrix V4 evidence

The earlier failed publication attempt was rejected because the Matrix report
was produced from the documentation descendant instead of artifact commit A.
That was corrected without editing evidence fields: an independent worktree at
commit A rebuilt the formal `0.0.1` A bundle, verified that all six JARs were
byte-identical to the current R/A bundle, executed all twelve process cases, and
received a fresh detached receipt from the external Matrix signer.

- A-worktree: `D:\MCAce-matrix-2a6274a`.
- A release bundle manifest SHA-256:
  `642977a69647391483336a80e320ffd6e6846df09de7b3d00e82444a00ce6dc2`.
- Cases: `12/12` passed (`10` stable, `2` beta); all process cleanup counts were
  zero; `release_eligible=true`.
- Trust-root SHA-256:
  `05be3d1ce14b03ab66db85045377f52afa91f03d662a4d70c26468a93bcde223`.
- Supervisor signer key: `mcace-matrix-v4-20260826-9f1c36f44eab`.
- Supervisor receipt SHA-256:
  `878ee99995f46fc63d02ea568dee1f16e11c504546f1bad49732fb912c8e53ad`.
- Matrix report SHA-256:
  `24b4760f5ad89ce755878b076229f47ac28f594dd7a34034987b561c31a0ed1c`.
- Published native index: [`server-version-process-matrix-20260907-2a6274a.json`](server-version-process-matrix-20260907-2a6274a.json).
- Published package contains the index plus six canonical documents and twelve
  raw case reports under
  `server-version-process-matrix/server-version-process-matrix-20260907-2a6274a/`.

The Matrix package is now the strongest source-bound external gate. It does not
close GUI consent, Federation V5, Vulcan V3, Production Authority V4, or
protected main/tag CI.

## Enable MCAce association check

The runtime probe subagent was notified that the operator had enabled MCAce. The
probe left the user's client PID `44064` untouched and performed a read-only
association check. The current client log had no matching MCAce consent,
`Loaded ModList`, resource-pack, `SERVER_CONFIRMED`, cheat/Xray, or upload
markers. Computer Use exposed only browser tabs (`apps=[]`), not the native
Minecraft window, so no screenshot or human GUI attestation was captured.

Therefore the operator's Enable statement remains an unbound pending runtime
observation. It is not promoted to release evidence, and no fixture was
generated to fill the gap.

A follow-up read-only monitor ran against the same client session without
stopping, attaching to, injecting into, or modifying PID `44064`:

- observation window: `2026-09-07T02:53:44.0023808+08:00` to
  `2026-09-07T02:54:32.1517116+08:00` (`48.1` seconds);
- the log grew from `197513` to `198754` bytes, but the appended content still
  contained no MCAce consent, `Loaded ModList`, resource-pack,
  `SERVER_CONFIRMED`, telemetry, report, or upload marker;
- result: `NO_MATCHING_NEW_MCACE_MARKERS`.

This follow-up does not change the gate status: the Enable statement remains
unbound and cannot be promoted to GUI or client-runtime release evidence.

## Release gate snapshot

Confirmed:

1. exact 1.21.11 / 26.1.2 / 26.2 product bundle and byte hashes;
2. controlled anti-cheat client/server correlation fixture with ModList and
   behavior signal;
3. fresh Matrix V4 A-package, 12/12 process cases, and non-fixture receipt;
4. static compatibility, artifact-policy, anti-cheat, federation-binding,
   Vulcan-wrapper, and process-matrix regression tests.

Still pending and fail-closed:

1. one visible human `Enable MCAce` consent plus signed PNG attestation;
2. real Fabric source-to-target Federation V5 handoff and post-run receipt;
3. licensed Vulcan JAR and genuine external provider event/receipt;
4. Production Authority V4 topology, raw package, and external receipt;
5. protected exact-commit `main` CI, then exact `v0.0.1` tag CI;
6. final README/README_CN evidence pass after the last exact R/A bundle rebuild.

MCAce remains a client telemetry/modlist/resource-pack/behavior correlation
system with server-side disposition. No Tencent ACE-equivalent kernel driver,
DMA detector, cross-process memory scanner, debugger/injection blocker,
public precision/recall, or automatic permanent ban is claimed.
