# Current candidate matrix: first case failed

Source and artifact-source: `15302cc70a51bd90a410b104f07761ec5c1f1101`.
The matching local release bundle built successfully (1m 13s), and the
three-version artifact compatibility contract passed. The previous candidate
was preserved under `build/release-history/5fc94a4-before-15302cc-20260908/`.

The real matrix execution passed input preflight and strict product compilation
(28 tasks executed). Its first case, `1.21.11-paper-velocity`, then failed.
JUnit recorded one test, zero skips, one failure, duration 68.698 seconds:
`java.io.IOException: proxy did not bind loopback listener`.
The invocation exited 1 (`SERVER_VERSION_MATRIX_GRADLE_FAILED`). No case is
accepted as passed; the remaining eleven cases were not executed.

Velocity loaded MCAce 0.0.1, but the available startup log did not show final
handshake initialization or a listener before the timeout. Source inspection
identified a misleading harness barrier: `startProxy()` waits for
`identity/server-public-key.txt`, claiming it is created after phase-2
initialization. In fact `onProxyInitialize()` calls `ServerIdentityStore.loadOrCreate`
before authority, federation, policy, handshake and command initialization.
The subsequent listener wait is only 30 seconds. The key file therefore does
not establish initialization completion. This explains why that barrier is
unsound; it does not yet prove why this particular startup took too long or
that increasing its timeout will resolve the failure.

The raw report records login/authentication false and no remaining owned
processes. No backend admission, inventory, cheat detection, GUI consent or
external supervisor receipt was verified. No signature was generated.

Evidence retained locally:

| File | SHA-256 |
| --- | --- |
| `build/matrix-15302cc-20260908.log` | `cb2f944de6d9ac6d417e901c91275e0f31d8365fb5435a78f1dbf25d4ead3fe5` |
| `mcace-runtime-integration/build/test-results/test/TEST-com.ellan.mcace.runtime.MinecraftProxyPlayerProbeTest.xml` | `6422febfa7709e0eac8a677619a880cf5af8aaaefa4bd0f6835985b948609020` |
| `build/runtime-player-probe/runs/velocity-2026-09-08T11-57-25-310561800Z/report.json` | `955ebeaf1f9c14f20a94585e5463c4572c9b58729a989615a3a4f58ab3f1cd02` |

Next: correct the startup-readiness barrier and obtain a fresh runtime result,
retaining this failure rather than replacing it with a success-only summary.
The release is still incomplete; no merge, tag or release was performed.
